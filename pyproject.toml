[project]
name = "cli-expense-tracker"
version = "0.1.0"
description = ""
authors = [
    {name = "anharmahebu",email = "anharmahebu28@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "questionary (>=2.1.1,<3.0.0)"
]

[tool.poetry]
packages = [{include = "cli_expense_tracker", from = "src"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[dependency-groups]
dev = [
    "pytest (>=9.1.1,<10.0.0)"
]
